# MongoDB Connection String
# For local: mongodb://localhost:27017/orders_db
# For Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/demo_db?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://your_connection_string_here

# Server Port (default: 5000)
PORT=5000

# Node Environment (development, production)
NODE_ENV=development

# Allowed CORS Origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=*

# Vercel (auto-set by Vercel)
# VERCEL=1
